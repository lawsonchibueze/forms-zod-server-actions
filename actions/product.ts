"use server";

// import connectToMongoDB from "@/lib/mongoose";
// import Product from "@/models/product.model";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function createProduct(p0: { productTitle: string; productDescription: string; productPrice: string; productImage: string; }) {
  try {
    
  } catch (error) {
    console.log(error)
    
  }
}

export async function getProducts() {
    try {
    
    } catch (error) {
      console.log(error)
      
    }
}